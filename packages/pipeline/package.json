{
  "name": "@spriteflow/pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Pure TypeScript sprite pipeline: input validation, detection, normalization, packing, export. Root entry is DOM-free; ./browser hosts worker/RPC adapters only.",
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.browser.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.browser.json --noEmit && tsc -p tsconfig.tests.json --noEmit",
    "bench": "node tools/bench.mjs",
    "check:contract": "node tools/check-contract.mjs",
    "test:stale": "node tools/repro-stale-result.mjs",
    "test:browser": "node tools/browser-smoke.mjs"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./browser": {
      "types": "./dist/browser/index.d.ts",
      "default": "./dist/browser/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "comlink": "4.4.2",
    "fflate": "0.8.3",
    "maxrects-packer": "2.7.3"
  }
}
