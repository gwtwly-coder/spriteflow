# SpriteFlow M1 集成验收裁决（第 2 轮）

> 日期：2026-09-23（Asia/Shanghai）  
> 验收官：本独立会话；执行手：R6 的其他会话  
> 被验 SHA：`caee0a403cb6b3a1bfb21421dab2800ecc8ce916`  
> 工作目录：`D:\projects\new_project1`  
> 本轮只新增本裁决书；不修改实现、测试、依赖、R6 报告及第 1 轮裁决书。

## 1. 裁决

**FAIL。**

接受 R6 的黄金 20/20、生产构建退出码 0 且 warning/error 均为 0，以及五次浏览器下载和已实现的 ZIP 基本结构断言通过。以上关闭对应证据缺口。

不接受将“七张真实素材的 Node 管线 7/7”计为 PRD 第 8 节的人工 UI 全流程退出标准。脚本直接调用管线并将检测结果全部设为 accepted，没有上传 UI、人工纠错或真实图浏览器下载；RW-04 和 RW-05 均直接导出建议网格的两帧。该结果证明管线允许审校确认后导出，不证明已把图片修正为引擎就绪素材。

阻断项包括：自动 components 结果错误显示为网格、未完成真实图人工审校退出验收、完整 P0/计划断言覆盖不足、Godot 4.4.x 实机验证缺失、性能证据和生产站关键路径仍缺。上一轮快捷键缺陷、许可文本缺失和跨浏览器快捷键缺测也未关闭。

本轮为第二次打回。**同阶段下一轮即第 3 轮，必须升级产品主人工裁决**，不得继续普通自动打回循环。R6 报告中的 FAIL / PENDING 是执行手状态，独立验收裁决仅为 FAIL，不采用 PENDING。

## 2. 取证方式与材料身份

本轮只读取报告、脚本、源码、夹具和图片；未重跑会写盘的 golden/build/Playwright/真实图脚本，没有安装或执行 Godot。R6 是现场执行者，本会话独立审查其执行范围与证据能支持的结论。

| 代号 | 路径与证据 |
|---|---|
| R | `tests/golden/reports/acceptance/exec-records.md`；第 3–10 行记录时间、SHA、Windows 11、Node 24.12.0、pnpm 11.9.0、Chrome 153.0.8010.53、临时 Playwright 1.63.0 |
| N | `tests/golden/reports/acceptance/p0-real-flow.mjs`；完整读取，共 216 行 |
| B | `tests/golden/reports/acceptance/playwright-smoke.mjs`；完整读取，共 209 行 |
| G | `tests/golden/reports/golden-report.json:3`；本次开始于 2026-09-23T11:37:04.710Z，pipeline 模式，20 通过、0 失败 |
| S07/S16 | `tests/golden/reports/acceptance/screenshots/07-scatter-5-godot.png`、`16-size-outlier-godot.png`；本会话直接查看 |
| V1 | `docs/acceptance/integration-verdict-1.md`；继承未关闭项及 CI/生产静态资源核验，不改写历史 |

材料 SHA-256：

- R：`596c3ec467519f2f67e9311606bdd89451af74d8ab1a4f39b5bd0ada5beef6d4`。
- N：`f6d52568961ece7713595add10724b8bd89c0f6ecbbdf638f0d68a4d784e1b22`。
- B：`694f7acbdd9de7b01b072de61b2af04de9f3946b2c05ce65471810330b178045`。
- V1：`cf1ba12345c19e8d13be1f4aa7846c418f5dbef05f4b3ed7759e0d3aad23579f`。

截图 S07/S16 可见 5 帧工作区和 Godot ZIP 导出完成；截图拍于导出后，侧栏被面板遮挡，不能单靠截图证明此前策略标签。策略错误由 R:151/153、B:74–80 的 DOM 读取和源码 `apps/web/src/app/App.tsx:965–968` 相互支持。

acceptance 目录未保留 ZIP、完整 stdout/stderr 日志或独立机器结果 JSON；下载在内存解包，R:160–164 保留了五个 ZIP 的名称、大小和哈希。本轮接受这些执行记录所明确覆盖的断言，不声称本验收官重新解包了不存在于目录中的 ZIP，也不将其当作真实图最终产物归档。

## 3. 通用集成检查单裁决

| 检查项 | 裁决 | 证据与变化 |
|---|---|---|
| 全部 PRD P0 标准 | FAIL | 第 7 节逐一覆盖 37 场景；自动策略标签实测失败，多项仍未执行完整标准。 |
| pnpm golden | PASS | R:23–47，退出码 0；G 为本轮 20/20。延续 V1 已核实的 CI #4 golden 成功。 |
| Playwright E2E 冒烟 | FAIL | R:118–167，7 次场景运行中 2 次标签失败、退出码 1；五次下载的基本结构断言通过。B 未覆盖计划要求的全部断言。 |
| 性能对照预算 | FAIL | R:80 证明体积通过；未提供 4K CCL 新测、UI 心跳/帧耗时/长任务和连续十次首次结果耗时。CI 绿色不能补足未执行指标。 |
| 构建零警告 | PASS | R:49–80：pnpm build 退出码 0，warning=0、error=0。按独立执行手记录接受；并非本验收官另行重建。 |
| Cloudflare 生产站关键路径 | FAIL | B:13 和 R:131–137 明确为 localhost preview，不是 `https://spriteflow-doa.pages.dev`。V1 的 HTTP 200/字节比对只证明静态资源可达。 |
| 许可证终审 | FAIL | R:70 的 213/213 清单一致通过；`THIRD_PARTY_NOTICES.txt:50–64` 仍仅有 Comlink Apache 简短声明，INT-07 未关闭。 |
| 1 Kenney + 5 AI 人工退出验收 | FAIL | R:94、N:67/99–108 是纯 API、全量 accepted；未完成真实图 UI 人工修正和产物记录。7 个 API 病例不能换算为 6/6 人工退出。 |

首屏体积为 92.5 KiB gzip（预算 <300 KiB），Worker 为 29.6 KiB gzip（目标 ≤180 KiB），子项 PASS。性能其余阈值和测量口径继承 `docs/architecture-m1.md:171–194` 及 `docs/prd-m1.md:448–450`。

## 4. 问题清单

INT-01～08 沿用首轮编号并更新范围；INT-09～11 为本轮明确定位或单独裁决。关闭的子项不再要求以相同理由重测。

| 编号 | 严重级 | 位置 | 问题描述 | 修复建议 |
|---|---|---|---|---|
| INT-01 | P0 阻断 | PRD 第 5 节；本文第 7 节；R:82–167 | 本轮不是全部 P0 的逐场景验收。8K、移动/八手柄、增删合拆排和完整 undo/redo、输入边界、内存恢复、离线/完整双语等仍无合格记录。 | R6 补齐第 7 节缺项，逐场景记录步骤、实际结果和原始产物/证据。不得用七例 API 自动确认代替 UI 操作。 |
| INT-02 | P0 阻断 | R:120/145/151/153；B:73–98、:117–149；`tests/e2e/PLAYWRIGHT-PLAN.md:28–56` | 已有可执行临时冒烟，关闭首轮“无执行记录”缺口，但矩阵仍失败；且没有角标筛选操作/断言，降级只验证应用后横幅消失，语言切换仅核帧数，Phaser 未解码 PNG 校验真实边界/规范化偏移与顺序。5/7 仅指脚本已实现的断言。 | 修复 INT-09 后按原计划补齐断言并复测；正确读取 ground truth 和导出契约，不能把脚本缩减范围当作全部计划通过。18/20 的 exports=[] 本身不是缺陷，但 P0 降级修正至导出仍须另测。 |
| INT-03 | P0 阻断 | N:67、:99–108；R:94、:102–106；`docs/prd-m1.md:452`；语义报告:46–68 | 真实图 7/7 是 API 回归。RW-04 两帧、RW-05 两帧、Kenney 整表一帧直接 accepted；RW-04/05 未执行语义修正。没有真实图 UI 上传/编辑/导出记录，不满足人工退出标准。 | R6 在浏览器实际审校固定的 1 Kenney+5 AI，逐例记录必要操作和最终产物。涉及 RW-04 时合并/重画为一完整帧，RW-05 人工恢复八帧；不要改自动降级 ground truth 来迎合语义目标。Kenney 明确预期切帧目标后执行。 |
| INT-04 | P0 阻断 | `docs/architecture-m1.md:182/190/194`；`docs/prd-m1.md:448–450`；R:49–80 | 只补了构建体积，没有 4096² CCL 与处理期间 UI 响应、首次结果速度的新报告。历史 Node 成绩不等于本轮浏览器/CI 门禁测量。 | R6/R7 按预算补参考环境、样本哈希、原始数据及 UI trace，在门禁报告中明确对应 SHA 和阈值。 |
| INT-05 | P0 阻断 | B:13；R:131–137 | 已关闭 golden/零警告构建缺口；剩余阻断仅为生产站关键路径未复测。localhost 的五次下载不能证明部署站可用。 | R6 在指定 Cloudflare 生产站完成上传、检测/降级、审校、三种导出解包与网络/离线检查，记录实际 URL 与资源/版本身份。 |
| INT-06 | P1 应修 | `apps/web/src/app/App.tsx:369/408/409/418`；V1 第 7.3 节 | 源码仍未隔离 Ctrl/Alt/Meta：Ctrl+S 同时切拆分、Ctrl+0 触发 fit、Alt+M 触发合并。新报告未修复或验证这些快捷键。 | R5 修复修饰键及焦点/模态作用域，R6 用实际浏览器补测。 |
| INT-07 | P1 应修 | `apps/web/public/THIRD_PARTY_NOTICES.txt:50–64`；`docs/architecture-m1.md:345` | Comlink 完整 Apache-2.0 许可文本仍缺失；213/213 包清单一致不能关闭发布声明缺口。 | R7 补完整许可并核对实际分发代码声明；R6 核对新发布物。完整许可依据及首轮生产核验见 V1。 |
| INT-08 | P1 应修 | `docs/ui-spec.md:566`；R:9；B:28 | Chrome 已从渲染冒烟推进到上传/导出 E2E，关闭“Chrome 仅渲染”描述；但 Chrome/Edge/Firefox 快捷键实测仍缺。 | Firefox 快捷键缺测维持可单独延后的 P1；其余键盘操作补测。P0 编辑/撤销标准不能随快捷键延后而豁免。 |
| INT-09 | P0 阻断 | `apps/web/src/app/App.tsx:965–968`；`docs/prd-m1.md:197–200`；R:151/153/177 | auto 请求得到 components 结果时，界面根据请求 mode 而不是实际 strategy 显示“网格”。07/16 实测失败，直接违反 AC-F05-A“标明采用的检测方式”。即使导出正确仍是 P0 标准未满足，不降为非阻断。 | R5 保存并展示当前 DetectResult.strategy，正确处理重新检测和手动结果替换；R6 复测 auto→grid/components、降级→manual 及中英显示。 |
| INT-10 | P1 应修 | `tests/e2e/fixtures/smoke-cases.json:35–40`；`tests/golden/cases/18-single-frame-degrade/ground-truth.json:18`；B:24 | E2E 夹具期望 1 帧，生效 ground truth 为 2。R6 本轮手写 2 帧绕开旧夹具，当前结果可采信但数据源仍分叉。 | R6/夹具 owner 同步旧期望，并让执行脚本读取 canonical ground truth，避免维护第三份帧数；保持已生效的降级标注。 |
| INT-11 | P0 阻断 | `docs/prd-m1.md:303–309`；R:182–197 | 本机没有 Godot 4.4.x，尚未执行脚本生成可加载 SpriteFrames、验证顺序/FPS/loop和目标已存在时拒绝覆盖。ZIP 结构不能证明引擎执行正确。 | 由具备 Godot 4.4.x 环境的 R6/独立执行会话补测并产出记录；验收官只裁决，不代替执行手安装或修改工程。环境缺失不构成降为 P1 或豁免的依据。 |

各证据缺口不等于产品已复现功能错误。INT-09 是本轮实测产品缺陷；INT-10 是已核实测试数据缺陷；INT-03 是验收方法和范围不满足退出标准，不是要求算法自动识别所有语义帧。

R:167 的一次未归因 404 保留为非阻断观察，不能未经请求 URL 证据就宣称是 favicon；本轮不据此推导下载失败或新增 P0。补测时保留资源 URL 即可。

## 5. 七张真实图记录的独立裁定

API 回归结果可接受为管线证据；人工 UI 退出验收为另一标准，不能互换。自动 ground truth 正确描述降级结果，语义复核描述人工完成后的目标，两者不冲突。

| 病例 | R6 已执行结果 | 可以证明 | 尚不能证明 |
|---|---|---|---|
| rw-02 | grid、6 帧、Hash ZIP | 管线 GT/结构回归通过 | 真实图 UI 上传、目视审校、最终产物记录 |
| RW-03 | 降级、2 帧、Hash ZIP | 默认降级及导出结构通过 | 人工判断桥接分割线与必要调整 |
| RW-04 | 降级、2 帧、Hash ZIP | 默认建议网格符合算法 GT | 单角色断件恢复为一个完整帧；R10 报告:46–52 明确要求此语义 |
| RW-05 | 降级、2 帧、Hash ZIP | 默认降级符合算法 GT | 八个语义动作帧的人工恢复及顺序；R10 报告:58–68 |
| RW-06 | grid、8 帧、Hash ZIP | GT、flags、导出结构通过 | UI 第八帧离群提示、人工确认且不删/缩放 |
| RW-07 | grid、4 帧、Hash ZIP | GT bbox/flags 与导出结构通过 | UI 审校中确认辉光边界与最终产物 |
| Kenney | 降级、1 帧、Hash ZIP，无 GT | 整表可作为单帧打包 | 素材表按预期切帧并经人工审校完成；不能以整表一帧替代该证明 |

N:99–108 将全部检测帧直接设为 accepted，没有执行合并、拆分、重画或重排。七张素材中未有一张提供该脚本之外的真实图 UI 人工执行记录，所以当前退出验收计数不是 7/7 或 6/6；判为未证明、FAIL。

## 6. Playwright 证据的可接受范围

- 本地 Chrome 七次 context 运行、五次下载：R:147–164；B:106–157 确认通过 download stream 读取实际 ZIP、内存解包并计算哈希，不是只检查出现下载按钮。接受已实现的文件集合、JSON 形状、帧数/引用及元数据边界断言通过。
- 07/16 的 strategyDisplayMatches=false 会使最终退出码 1（B:192/203–209），没有把这些失败吞掉。
- 13/16/20 在 B:18–26 没有 expectedReviewFlag，正文无按角标筛选操作或断言；不能由帧数正确证明 F18 和计划中的筛选通过。
- B:95–97 只验证切语言后帧数仍正确；没有检查英文字串、所有状态、历史保存，不能证明完整 F01-B。
- B:82–90 只在降级例应用默认网格后检查帧数和横幅消失，没有确认初始 EMPTY_INPUT 原因/提示、角标或修改行列与手动画框后再导出的闭环。夹具 exports=[] 允许这两例不下载，但 P0 的相关闭环仍需其他实测覆盖。
- B:117–126 对 Godot 校验文件集合、数量、schema 和文件引用；未解码 PNG 对照统一逻辑画布/offset，也未运行脚本。
- B:129–149 的 Phaser 物理边界按 JSON meta 检查，未解码浏览器实际下载的 atlas PNG，未比较完整帧顺序与规范化 preview 的 sourceSize/spriteSourceSize。这是可接受的基本冒烟，不能上升为全部 F09/F10 断言已覆盖。
- B:170–171 检查同源 GET；R:167 记录仅四个静态请求、无素材上传。接受本地七例执行范围内的网络观察；没有离线复测，也不是六张真实图/生产站的隐私验收。

## 7. 37 个 P0 场景逐条裁决

标准位置均为 `docs/prd-m1.md`。PASS 仅表示该场景整体已满足；获得部分证据但未覆盖完整 Then 的场景仍为 FAIL（证据不足），不是认定功能已坏。纯算法场景接受本次真实运行的黄金回归，不额外强加浏览器前提：runner 按帧索引比较 bbox，并校验 strategy、数量、included、pending 和 flags（`tests/golden/tools/run.mjs:170–218`）。明确要求 UI、人工操作或引擎执行的场景不能由该回归替代。

| 场景 | PRD 位置 | 裁决 | 本轮证据与未满足部分 |
|---|---|---|---|
| F01-A | :120 | FAIL | R:167/B:170–171 支持本地无上传；离线处理、编辑链路及部署站未复测。 |
| F01-B | :125 | FAIL | B:95–97 只验帧数，不验各主状态文案/错误/对话框及历史。 |
| F02-A | :132 | FAIL | R:149–150 PNG 拖放通过；WebP、文件名/尺寸完整断言不足。 |
| F02-B | :138 | FAIL | R:151–155 PNG 点选通过；WebP 和两入口同等行为完整覆盖不足。 |
| F02-C | :143 | FAIL | 无非 M1、多个文件、全不透明输入拒绝记录。 |
| F02-D | :149 | FAIL | 无原始 99%/略高边界实测。 |
| F02-E | :157 | FAIL | 无损坏输入、清理和重新上传实测。 |
| F03-A | :165 | PASS | G 中 01–04 网格样例，精确策略/数量及逐索引 bbox IoU 校验通过；B:73 补充 01 四帧 UI 观察。 |
| F03-B | :170 | PASS | G 中 05/06 条带样例通过；N/R:100 补充真实 rw-02 六帧结果。 |
| F03-C | :175 | PASS | G 中 13/14 检验保留数量、逐框 bbox、multipleComponents、included=true、pending；13 六帧浏览器确认及下载通过。UI 角标完整呈现另归 F18-A。 |
| F04-A | :185 | PASS | G 中 07/08 散排、11/12 噪点样例检验策略/数量/逐框 IoU；07 五帧浏览器结果支持。策略文案错误另归 F05-A。 |
| F04-B | :190 | PASS | G 中 09/10 帽子/武器断件样例，帧数及 bbox/flags 匹配，断件合并算法要求已验证。 |
| F05-A | :197 | FAIL | 07/16 components 被显示为网格；R:151/153，源码:967；实测缺陷。 |
| F05-B | :202 | FAIL | 18/20 可应用手动网格；初始低置信度提示、非成功语气和预填内容未完整断言。 |
| F05-C | :207 | FAIL | B 只应用默认网格；没有调整行列、增拆删修正并导出。 |
| F05-D | :212 | FAIL | 20 一帧且应用网格后可确认；未完整验证初始 EMPTY_INPUT 提示/空帧 pending 及编辑动作。 |
| F06-A | :222 | FAIL | 无 fit/zoom/pan、坐标对齐和尺寸同步实测。 |
| F06-B | :227 | FAIL | 无满足内存条件路径下 8192² 浏览器交互记录。 |
| F07-A | :234 | FAIL | 无移动/八个手柄逐项实测。 |
| F07-B | :239 | FAIL | 无增删合拆操作及画布/列表/导出同步实测。 |
| F07-C | :244 | FAIL | 无第三帧拖到首位并验证预览/导出顺序。 |
| F08 | :251 | FAIL | 无完整编辑历史逐步 undo/redo、端点禁用实测。 |
| F09-A | :258 | FAIL | N 有规范化调用；未对像素、offset 和 UI preview 做对应核验。 |
| F09-B | :266 | FAIL | 五次下载基本结构通过；缺 padding/extrude、物理像素和偏移逐像素核验。 |
| F09-C | :273 | FAIL | 20 浏览器没有导出，缺空帧占位/逻辑画布/时序完整实测。 |
| F10-A | :283 | FAIL | 01/13 Hash 基本下载解包通过；缺浏览器 PNG 实际边界与完整审校顺序对照。 |
| F10-B | :288 | FAIL | 01 Array 基本下载解包通过；完整几何/顺序核验同上。 |
| F11-A | :297 | FAIL | 07/16 Godot 文件集合/数量/引用通过；PNG 统一画布、offset 和预览对应未检。 |
| F11-B | :303 | FAIL | R:195 明确无 Godot 4.4.x，脚本实际执行和拒绝覆盖未测；INT-11。 |
| F12-A | :313 | FAIL | 无 >8192 降采样/取消路径实测。 |
| F12-B | :318 | FAIL | 无预估内存不足阻断和降低尺寸重试实测。 |
| F12-C | :323 | FAIL | 无运行时分配失败注入、数据保留和恢复实测。 |
| F13-A | :330 | PASS | G 20 例 pipeline 通过，继承 V1 数据集/标注覆盖核验。 |
| F13-B | :337 | PASS | R:33/42、G 20/20；当前 SHA 的 CI #4 成功已于 V1 独立核实。 |
| F18-A | :344 | FAIL | S16 可见复核提示；未逐帧核三类角标、同时显示和不改数据。 |
| F18-B | :352 | FAIL | B 无筛选操作/断言，不足以证明筛选不删帧、不重排。 |
| F18-C | :360 | FAIL | B:93–94 证明能确认；缺 flags/included/顺序在确认前后的完整比较，20 未导出。 |

## 8. 本轮关闭与后续人工裁决材料

已关闭：INT-05 中零警告构建证据缺口；INT-02 中“没有任何 Playwright 执行”的缺口；INT-08 中“Chrome 只有渲染冒烟”的旧描述。黄金门禁维持 PASS。七张真实图 API 回归、五次下载基本结构成功保留为有效的部分证据，不否定已完成工作。

未关闭：INT-01～08 的剩余范围以及 INT-09～11。Godot 环境缺失明确裁为 P0 阻断，不能通过“移交验收官”自动免验；具备环境的执行会话应实际运行，再由验收官审查记录。

第 3 轮交产品主时，应同时提供：修复后的源码 SHA；INT-09/10/06/07 对应修复与回归；真实图人工审校和最终产物；完整 P0/E2E 覆盖；Godot 4.4.x 运行与拒绝覆盖；4K/UI 性能；生产站关键路径。保留 R6 原始报告，本裁决不要求把成功记录改写成失败，也不允许把未执行项写成 PASS。
